---
- scm:
    name: git-ui-tests
    scm:
      - git:
          url: git@github.com:Tretjakoff/Otus_project_first.git
          credentials-id: jenkins
          branches:
            - FETCH_HEAD
          refspec: ${REFSPEC}
